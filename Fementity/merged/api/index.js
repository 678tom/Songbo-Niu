// const http = require('http');
// const app = require("./src/app");

// const { PORT = 4000 } = process.env;

// http.createServer(app).listen(PORT, async (err) => {
//   if (err) throw err;
//   console.log(`🚀 Server ready at http://localhost:${PORT}`);
// } );


